# deps / build
node_modules/
dist/
*.tsbuildinfo

# secrets & local env — never commit (and never let a cloud handoff carry these)
.env
.env.*
!.env.example
*.key
*.pem
*.pfx
*.p12
secrets/

# runtime artifacts
*.log
audit.jsonl
*.audit.jsonl
# test/smoke audit artifacts (_audit_test.jsonl, …)
_*.jsonl
# test/smoke policy-persistence scratch files (_policy_unit.json, …)
_policy_*.json
# optional file-backed tenant policy (POLICY_STORE)
policies.json
coverage/

# os cruft
.DS_Store
Thumbs.db
